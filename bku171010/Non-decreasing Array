#요는 array[i] > array[i+1] 구간이 2개 미만이면 True, 2개 이상이면 False

class Solution:
    def checkPossibility(self, nums: List[int]) -> bool:

        Count = 0    #감소구간

        #for 문으로 nums[i] 와 nums[i+1] 을 비교
        # if nums[i] > nums[i+1] 라면
        # Count = Count +1
        # Count가 2 되면 브레이크 및 False 반환
        # for 문이 완료되었음에도 Count가 2보다 작다면 True 반환
