# cnt = 0

# scoville 리스트에서 K 보다 작은 수가 있는지 확인
# 없다면 return cnt

# 있다면 리스트 오름차순 정렬
# 0, 1번째 인덱스 pop 후 [0] + [1] x2 하여 리스트에 append
# 리스트에서 K 보다 작은 수가 있는지 확인
# 없다면 return cnt

# 있다면 위 과정 반복

# 더이상 더할 수가 없고, 원소가 K 보다 작다면 return -1
